# wbproxy Zeabur 部署记录（2026-09-15）

## 已部署组件

| 项 | 值 |
|---|---|
| Zeabur 项目 | Ai_Proxy（69422387c95d806d08bfcdf3） |
| 服务 | wbproxy-go（6aa83cd6f8d2260da1258f3d） |
| 状态 | RUNNING（跑在你自己的 RAKsmart VPS K3s 上，137.175.102.24） |
| 域名 | wbproxy.bulalabu.xyz（已绑定，需在 Cloudflare 加 A 记录 → 137.175.102.24） |
| 代码 | github.com/limin640/wbproxy（私有） |
| 环境变量 | AUTH_JSON（登录态）、GATE_KEY=sk-wb-work-2026 |

## New API 渠道配置

| 字段 | 值 |
|---|---|
| 类型 | OpenAI |
| Base URL | https://wbproxy.bulalabu.xyz |
| 密钥 | sk-wb-work-2026 |
| 模型（对外） | deepseek-v4.1-flash-work |
| 模型映射 | {"deepseek-v4.1-flash-work": "deepseek-v4.1-flash"} |

## 当前阻塞（非代码问题）

**上游腾讯网关对 token 的 chat 通道临时软风控**：今天联调测试请求过频，
`POST /v2/chat/completions` 对该 token 挂起（GET / 正常，非 IP 封锁，本机与
VPS 两侧同时挂起）。特征是挂起而非 401/429。通常几小时到一天自动解除。

解除后无需任何操作，链路自动全通（服务 RUNNING、门禁生效、/v1/models 正常）。

**验证方法**：
```bash
curl --resolve wbproxy.bulalabu.xyz:443:137.175.102.24 -k \
  https://wbproxy.bulalabu.xyz/v1/chat/completions \
  -H "Authorization: Bearer sk-wb-work-2026" \
  -H "Content-Type: application/json" \
  -d '{"model":"deepseek-v4.1-flash","messages":[{"role":"user","content":"hi"}]}'
```

**待办**：Cloudflare 面板给 wbproxy.bulalabu.xyz 加 A 记录 → 137.175.102.24
（当前测试用 --resolve 绕过 DNS）。

## 排障命令

```bash
# 服务状态
curl -sS -X POST https://api.zeabur.com/graphql \
  -H "Authorization: Bearer $(python3 -c "import yaml;print(yaml.safe_load(open('$HOME/.config/zeabur/cli.yaml'))['token'])")" \
  -H "Content-Type: application/json" \
  -d '{"query":"query{ service(_id: \"6aa83cd6f8d2260da1258f3d\"){ deployments { _id status } } }"}'

# 上游探测（容器内连 workbuddy.ai）
curl -k --resolve wbproxy.bulalabu.xyz:443:137.175.102.24 \
  https://wbproxy.bulalabu.xyz/debug/upstream

# 重新部署（本机目录内）
pnpm dlx zeabur@latest deploy --service-id 6aa83cd6f8d2260da1258f3d \
  --environment-id 694223874947dd57c4fd039b
```

## 重要教训（已记住）

1. **token 别频繁直调**：workbuddy.ai 上游对同 token 高频 chat 请求做软风控
   （挂起）。调试间隔拉长，一次调试 ≤10 次。
2. **Zeabur API 的坑**：
   - spec.source.dockerfile 字段（字符串）会被 zbpack-v2 builder 写进
     bootstrap Dockerfile 的指令区 → "unknown instruction: Dockerfile"。
     Dockerfile 路径让 Zeabur 自动探测（仓库根放 Dockerfile 即可）。
   - wbproxy 二进制的 flag 不能叫 -key/-listen（zbpack /bin/server 占用），
     已改为 -wb-gate/-wb-listen/-wb-auth/-wb-upstream。
   - deleteService 参数是 `_id` 不是 serviceID。
3. **token 刷新**：wbproxy 容器会自动刷新并只在内存/日志里保留，auth.json
   是导出快照。若桌面端重新登录，需重新导出 auth.json 并更新 Zeabur 环境变量。
