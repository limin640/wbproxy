# wbproxy Zeabur 部署记录（2026-09-15，全线打通 ✅）

## 最终状态

| 项 | 值 |
|---|---|
| wbproxy 服务 | Zeabur 项目 Ai_Proxy / 服务 wbproxy-go（6aa83cd6f8d2260da1258f3d），RUNNING |
| 运行位置 | 自有 RAKsmart VPS K3s（137.175.102.24），7×24，与 Mac 无关 |
| 代码 | github.com/limin640/wbproxy（私库） |
| 域名 | wbproxy.bulalabu.xyz，CF DNS only（A → 137.175.102.24），Let's Encrypt 证书自动签发 |
| 生产验证 | ✅ chat 返回「成功」、门禁 401 生效、/health 正常 |
| newapi 渠道 | 用户已手动在 kimi.bulalabu.xyz 添加（模型 deepseek-v4.1-flash-work → 映射 deepseek-v4.1-flash） |

## 使用方式（最终形态）

```
任意工具 → https://kimi.bulalabu.xyz/v1（现有 newapi key）
           模型名: deepseek-v4.1-flash-work
```

## 日常运维

```bash
# 服务状态
curl -sS -X POST https://api.zeabur.com/graphql \
  -H "Authorization: Bearer $(python3 -c "import yaml;print(yaml.safe_load(open('$HOME/.config/zeabur/cli.yaml'))['token'])")" \
  -H "Content-Type: application/json" \
  -d '{"query":"query{ service(_id: \"6aa83cd6f8d2260da1258f3d\"){ deployments { _id status } } }"}'

# 重新部署（在 workbuddy-proxy 目录内）
pnpm dlx zeabur@latest deploy --service-id 6aa83cd6f8d2260da1258f3d \
  --environment-id 694223874947dd57c4fd039b

# 上游连通探测
curl https://wbproxy.bulalabu.xyz/debug/upstream
```

## 教训清单（重要）

1. **上游软风控**：workbuddy.ai 对同 token 高频 chat 挂起（非 429）。调试一次
   ≤10 发。风控约几小时解除。
2. **token 刷新后会话失效**：wbproxy 容器自动刷新 token 后，若上游会话没同步
   会 401。修法：重新导出 auth.json → 更新 Zeabur 环境变量 AUTH_JSON →
   redeploy。桌面端重新登录后同样操作。
3. **Zeabur API 坑**：
   - spec.source.dockerfile 字符串会被 zbpack-v2 写进 bootstrap Dockerfile
     指令区（unknown instruction）。仓库根放 Dockerfile 让它自动探测。
   - flag 不能叫 -key/-listen（zbpack /bin/server 占用）→ 用 -wb-gate 等前缀。
   - deleteService 参数 `_id`；updateEnvironmentVariable 用 `data: Map`。
   - GraphQL 网关用 api.zeabur.com（gateway.zeabur.com 证书错误）。
   - token 在 ~/.config/zeabur/cli.yaml（zat_ 开头，CLI 登录一次管很久）。
4. **DNS**：bulalabu.xyz 在 Cloudflare（账号 291045649limin@gmail.com，Google
   登录，ego-browser 有登录态）。DNS only + K3s ingress 自动 Let's Encrypt。
   首次访问若自签证书，等 1-2 分钟证书签发完成。
