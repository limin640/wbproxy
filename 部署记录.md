# wbproxy Zeabur 部署记录（2026-09-15，已全线打通 ✅）

## 已部署组件

| 项 | 值 |
|---|---|
| Zeabur 项目 | Ai_Proxy（69422387c95d806d08bfcdf3） |
| 服务 | wbproxy-go（6aa83cd6f8d2260da1258f3d） |
| 状态 | RUNNING（跑在你自己的 RAKsmart VPS K3s 上，137.175.102.24） |
| 代码 | github.com/limin640/wbproxy（私有） |
| 环境变量 | AUTH_JSON（登录态）、GATE_KEY=sk-wb-work-2026 |
| 域名 | wbproxy.bulalabu.xyz（已绑 Zeabur，**Cloudflare 还需加 A 记录 → 137.175.102.24**） |

## 全链路验证结果 ✅

```
工具 → kimi.bulalabu.xyz（newapi）→ wbproxy 渠道 → wbproxy.bulalabu.xyz → www.workbuddy.ai
```

- wbproxy 生产 chat：✅「成功」（2026-09-15 03:22）
- 门禁密钥：✅ 错 key 401
- /v1/models：✅ 含 deepseek-v4.1-flash-work

## New API 渠道配置（面板里加渠道用）

| 字段 | 值 |
|---|---|
| 类型 | OpenAI |
| 名称 | WorkBuddy(国际版) |
| Base URL | https://wbproxy.bulalabu.xyz |
| 密钥 | sk-wb-work-2026 |
| 模型（对外） | deepseek-v4.1-flash-work |
| 模型映射 | {"deepseek-v4.1-flash-work":"deepseek-v4.1-flash"} |
| 分组 | default |

## 待办

1. Cloudflare 面板：wbproxy.bulalabu.xyz 加 A 记录 → 137.175.102.24（不加 DNS，
   newapi 服务器解析不了这个域名，渠道测试会挂；测试期可用 --resolve 绕过）
2. newapi 面板加上面的渠道
3. 之后所有工具统一走 kimi.bulalabu.xyz/v1，模型填 deepseek-v4.1-flash-work

## 排障命令

```bash
# 服务状态
curl -sS -X POST https://api.zeabur.com/graphql \
  -H "Authorization: Bearer $(python3 -c "import yaml;print(yaml.safe_load(open('$HOME/.config/zeabur/cli.yaml'))['token'])")" \
  -H "Content-Type: application/json" \
  -d '{"query":"query{ service(_id: \"6aa83cd6f8d2260da1258f3d\"){ deployments { _id status } } }"}'

# 上游探测（容器内连 workbuddy.ai）
curl -k --resolve wbproxy.bulalabu.xyz:443:137.175.102.24 \
  https://wbproxy.bulalabu.xyz/debug/upstream

# 重新部署（本机项目目录内）
pnpm dlx zeabur@latest deploy --service-id 6aa83cd6f8d2260da1258f3d \
  --environment-id 694223874947dd57c4fd039b
```

## 重要教训（已记住）

1. **token 别频繁直调**：workbuddy.ai 上游对同 token 高频 chat 做软风控（挂起
   而非 429）。调试一次 ≤10 发，间隔拉长。
2. **token 刷新后会话失效**：容器自动刷新 token 后，新 token 需要同步回
   Zeabur 环境变量并重新部署，否则旧会话 401。修正流程：
   ```bash
   # 桌面端重新登录后，导出最新登录态并更新环境变量，再 redeploy
   ```
3. **Zeabur API 坑**：
   - spec.source.dockerfile 字符串字段会被 zbpack-v2 写进 bootstrap
     Dockerfile 指令区 → "unknown instruction"。让 Zeabur 自动探测仓库根
     Dockerfile，别传这个字段。
   - wbproxy flag 不能叫 -key/-listen（zbpack /bin/server 占用），已改为
     -wb-gate/-wb-listen/-wb-auth/-wb-upstream；端口默认读 $PORT。
   - deleteService 参数是 `_id`；updateEnvironmentVariable 用 data: Map。
   - Zeabur GraphQL 网关：api.zeabur.com（gateway.zeabur.com 证书错误）。
4. **本机零依赖**：wbproxy 全部跑在服务器；Mac 只在桌面端重新登录时导出一次
   auth.json（项目目录 auth.json，更新到 Zeabur 环境变量 AUTH_JSON 即可）。
